const initState = {
    token : '',
    username : '',
}

const globalReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default globalReducer;